extends layout.pug 

block content 

    .container 
        h1 All Notes 

        each note in notes 
            div.note-wrapper
                a.note(href=`/notes/${ note.id }`) #{ note.title }
                a.delete(href='#') Delete